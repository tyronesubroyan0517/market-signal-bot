# This build runs entirely on mock/simulated data — no keys required to deploy.
# Reserved for when a real data provider is connected later (see README).

# VITE_DATA_PROVIDER_API_KEY=
# VITE_LIVE_TRADING_ENABLED=false
